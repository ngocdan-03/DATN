server:
  port: 8080
  servlet:
    context-path: /real-estate
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/real_estate_db
    username: root
    password: ${DB_PASSWORD}
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      connection-timeout: 5000
  mail:
    host: smtp.gmail.com
    port: 587
    username: buingocdan2005@gmail.com
    password: ${MAIL_APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
jwt:
  signerKey: ${JWT_SIGNER_KEY}
  valid-duration: 20 #second
  refresh-duration: 360 #second