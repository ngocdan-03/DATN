package com.NgocDan.BACKEND.enums;

import lombok.Getter;

@Getter
public enum PropertyType {
    // Đất nền, đất thổ cư
    LAND,
    // Nhà nguyên căn, nhà phố, biệt thự
    HOUSE,
    // Căn hộ chung cư, studio
    APARTMENT
}
